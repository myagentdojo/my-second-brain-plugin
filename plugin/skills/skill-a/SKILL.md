---
name: skill-a
description: "Run the compiled skill-a proof with ESM and CJS dependencies offline on macOS arm64."
---

# Skill A

Resolve the installed plugin root two directories above this `SKILL.md`, then run its `bin/skill-a` launcher.

Report the JSON result. If the launcher returns an error envelope, report its cause and rebuild or reinstall guidance. This compiled skill includes Bun and its dependencies for macOS arm64; runtime download or repair cannot fix its payload.
