# New Skill Formation Source Guidance

- Keep `SKILL.md` as the workflow owner; point to native owners instead of copying their mechanics.
- Keep the Bare Skill Shell minimal, then route immediately through `grill-with-docs`; Domain Modeling creates `CONTEXT.md` and its `CONTEXT-MAP.md` row together only when the first distinct skill-local term resolves.
- Keep `CONTEXT.md` as a pure Domain Modeling glossary. Keep `AGENTS.md` concise and source-facing; derive `CODING_STANDARDS.md` late from accepted design and witnessed evidence.
- Preserve explicit checkpoint names, artifact maturity, and the stop-before-implementation boundary.
- Keep model-only formation in the Plugin Payload unless an accepted Complexity Gate and Architecture Shell authorize a package shell.
- Keep the repository-pattern test discovery-only and use an independent expected result.
- Use Writing for Agents for skill prose, Domain Modeling for vocabulary, and Test Design for test routing and proof.
- Preserve cross-harness source paths and native development-owner boundaries.
